<div class="about-container">
  <ul>
    <li><%=link_to "ABOUT", "#linktoabout", :class=>"nav-link"%></li>
    <li><%=link_to "CODE SNIPPETS", "#linktosnippets", :class=>"nav-link"%></li>
    <li><%=link_to "WEB APPS", "#linktowebapps", :class=>"nav-link"%></li>
    <li><%=link_to "SKILLS & TOOLS", "#linktoskillstools", :class=>"nav-link"%></li>
  </ul>

  <div class="about">
    <%=image_tag("me-copy.jpg", :class=>"img")%>
    <h1>Jorge Hernandez</h1>
    <h2>ASPIRING SOFTWARE ENGINEER</h2>
    <hr class="grey">
    <p>Jorge is someone who is consistently growing themselves and who takes the time to continue learning even though it's not a direct requirement of the job.  I find that many times it's a combination of what you study both directly and indirectly related to work. I seek to work for a successful company that has strong leadership and vision and who recognizes and rewards performers</p>
  </div>
</div>

<h3 id="linktosnippets">CODE SNIPPETS</h3>
<hr class="aqua">

<div class="box">
  <h4>Temperature Conversion</h4>
  <p>
  This ruby program will prompt the user for a temperature in degrees Celsius and let the user know what the corresponding temperature is in Farehneit.
  </p>
</div>

<div class="box">
  <pre><code class="language-ruby">
  puts <span class="token string">"Enter a temperature in degrees Celsius:"</span>
  celsius = gets.chomp
  c = celsius.to_f

  farenheit = c * <span class="token number">1.8</span> + <span class="token number">32</span>
  puts <span class="token comment" spellcheck="true">"<span class="interpolation">#{</span>c<span class="interpolation">}</span> degrees Celsius is equivalent to</span>
  <span class="token comment" spellcheck="true"><span class="interpolation">#{</span>farenheit<span class="interpolation">}</span> degrees Farenheit."
  </span>

  <span class="keyword">def</span> c2fconversion(c)
    <span class="token keyword">return</span> c * <span class="token number">1.8</span> + <span class="token number">32</span>
  <span class="keyword">end</span>

  <span class="var">f</span> = <span class="var">c2fconversion(c)</span>
  puts <span class="token comment" spellcheck="true">"<span class="interpolation">#{</span>c<span class="interpolation">}</span> degrees Celsius is equivalent to</span>
  <span class="token comment" spellcheck="true"><span class="interpolation">#{</span>farenheit<span class="interpolation">}</span> degrees Farenheit."</span>
  </code></pre>
</div>

<br class="clear" />

<div class="box">
  <pre class="language-ruby"><code class="language-ruby">
  puts <span class="token string">"Enter a number:"</span>
  input = gets.chomp.to_i
  num = input

  <span class="token keyword">while</span> num > <span class="token number">100</span>
    num = num % <span class="token number">100</span>
  <span class="token keyword">end</span>

  <span class="token keyword">case</span> num
  <span class="token keyword">when </span><span class="token number">11</span>, <span class="token number">12</span>, <span class="token number">13</span>
    puts input.to_s + <span class="token string">"th"</span>
  <span class="token keyword">else</span>
    num = num % <span class="token number">10</span>

    <span class="token keyword">case</span> num
    <span class="token keyword">when</span> <span class="token number">1</span>
      puts input.to_s + <span class="token string">"st"</span>
    <span class="token keyword">when</span> <span class="token number">2</span>
      puts input.to_s + <span class="token string">"nd"</span>
    <span class="token keyword">when</span> <span class="token number">3</span>
      puts input.to_s + <span class="token string">"rd"</span>
    <span class="token keyword">else</span>
      puts input.to_s + <span class="token string">"th"</span>
    <span class="token keyword">end</span>
  <span class="token keyword">end</span>
</code></pre>
</div>
<div class="box">
  <h4>Ordinal Challenge</h4>
  <p>This ruby program will convert a plain number to the ordinal of the number. For example, if the user enters 2, it will display 2nd; if the user enters 3, it will display 3rd, etc.</p>
</div>

<br class="clear">

<div class="box">
  <h4>Foobar</h4>
  <p>This ruby program is an adaptation of a classic technical interview problem which displays sequences of the Foobar pattern.</p>
</div>
<div class="box"><pre class="language-ruby">
<code class="language-ruby">
  <span class="token keyword">def</span> foobar(range)
    range.<span class="token keyword">each</span> <span class="token keyword">do</span> |foo|
    <span class="token keyword">if</span> foo % <span class="token number">3</span> == <span class="token number">0</span> && foo % <span class="token number">5</span> != <span class="token number">0</span>
      puts <span class="token string">"Foo"</span>
    <span class="token keyword">elsif</span> foo % <span class="token number">5</span> == <span class="token number">0</span> && foo % <span class="token number">3</span> != <span class="token number">0</span>
      puts <span class="token string">"Bar"</span>
    <span class="token keyword">elsif</span> foo % <span class="token number">3</span> == <span class="token number">0</span> && foo % <span class="token number">5</span> == <span class="token number">0</span>
      puts <span class="token string">"Foobar"</span>
    <span class="token keyword">else</span>
      puts foo
    <span class="token keyword">end</span>
    <span class="token keyword">end</span>
  <span class="token keyword">end</span>

  puts <span class="token string">"How many digits would you like to see?"</span>
  x = gets.chomp.to_i
  range = (<span class="token number">1</span>..x).to_a
  foobar(range)
  </code></pre>
</div>

<br class="clear">

<div>
  <h3 id="linktowebapps">WEB APPS</h3>
  <hr class="aqua">
  <div class="box">
    <h4>Quote Generator</h4>

  </div>
  <div class="box"></div>
  <div class="box"></div>
  <div class="box"></div>
  <div class="box"></div>
  <div class="box"></div>
</div>

<div>
  <h3 id=linktoskillstools>SKILLS & TOOLS</h3>
  <hr class="aqua">
  <p>These are my skills and tools!</p>
</div>
